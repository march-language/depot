[package]
name = "depot"
version = "0.1.0"
type = "lib"
description = "A composable ORM toolkit for March — schemas, queries, validation, migrations, and a built-in PostgreSQL wire client"
author = "Chase Gilliam"
license = "MIT"

[deps]

[ffi]
sources = ["native/sqlite_shim.c"]
link    = ["-lsqlite3"]
